# Tome 静态站点配置模板：入口脚本按 WEB_PORT 替换占位符后写入 /etc/nginx/http.d/tome.conf
server {
    listen __WEB_PORT__;

    root /app/out;
    index index.html;

    # Next 静态导出每个路由都有对应 .html；未知路径回 404.html
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    error_page 404 /404.html;
}
